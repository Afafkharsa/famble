<nav class="navbar navbar-expand-sm navbar-light navbar-lewagon <%= yield(:navbar_class) %>">
  <div class="container-fluid">
    <% if current_user && current_user.role %>
      <%= link_to root_path, class: "navbar-brand h3 p-0 m-0" do %>
        Famble
      <% end %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
              data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
              aria-expanded="false" aria-label="Toggle navigation" >
        <span class="navbar-toggler-icon"></span>
      </button>
    <% else %>
      <%= link_to root_path, class: "navbar-brand h3 p-0 m-0 text-white" do %>
        Famble
      <% end %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
              data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
              aria-expanded="false" aria-label="Toggle navigation"
              data-bs-theme="dark">
        <span class="navbar-toggler-icon"></span>
      </button>
    <% end %>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "My Meal Plans", meal_plans_path, class: "nav-link" %>
          </li>
          <li class="nav-item border-end border-2">
            <%= link_to "My Recipes", recipes_path, class: "nav-link" %>
          </li>
          <li class="ms-2 nav-item dropdown link-underline-opacity-0  d-flex align-items-center ">
            <a href="#" class="nav-item" id="navbarDropdown"
               data-bs-toggle="dropdown" aria-haspopup="true"
               aria-expanded="false">
              <i class="fa-regular fa-circle-user fs-4 text-info"></i>
            </a>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <div class= "dropdown-item" ><%= current_user.name %></div>
              <hr class="dropdown-divider">
              <%= link_to "My family", families_path, class: "dropdown-item"  %>
              <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete},
                  class: "dropdown-item" %>
            </div>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
