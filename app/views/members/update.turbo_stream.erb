
<%# 1. Update new member's details on the same ID %>
  <%= turbo_stream.replace dom_id(@member), partial: "members/member", locals: { member: @member } %>
  <%#= turbo_stream.replace member_list(@member), partial: "members/member", locals: { member: @member } %>


<%# 2. Clear data's Modal and reset SRC %>
  <%= turbo_stream.replace "modal" do %>
    <turbo-frame id="modal"></turbo-frame>
  <% end %>
